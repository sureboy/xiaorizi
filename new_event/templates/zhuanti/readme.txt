专题的模板放在了文件夹：new_event/templates/zhuanti
新的模板放在该文件夹下即可。
模板名称为：专题名（英文字母）.html，默认模板为event.html。

后台活动编辑页面，“标题缩写”处即为专题名。

具体的活动页面由new_event/views.py中的showPage控制，从url中得到专题名，判断专题名是否和数据库中的专题名一致：
	1）不一致，输出404页面
	2）一致，在模板文件夹下寻找对应专题模板：
		a）找到，则使用
		b）没找到，使用默认

